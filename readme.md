# Robot Challenge - 16th May 2024

## Usage

Run the following command to start the simulator:
```sh
ruby -r './main.rb'
```

Run the following command to run the tests:
```sh
bundle install
bundle exec './bin/rspec spec'
```
